name: Test Trigger

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    labels: [ k8s-master-node ]
    steps:
      - run: echo "✅ Workflow triggered successfully!"
